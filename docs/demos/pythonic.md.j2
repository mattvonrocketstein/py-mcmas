{% import 'macros/base.j2' as macros -%}{% import 'macros/site.j2' as site -%}

## Pythonic ISPL
<hr style="width:100%;border-bottom:2px solid black;">

Using `py-mcmas`, you can write ISPL specifications directly in python in a straightforward way.  There's a bunch of reasons you might want to do this, but the simplest reason is that ISPL specifications are more like *data* than like programs anyway.  

* Explicitly using [logical operators](#logical-expressions) instead of strings is frequently harder to *read*, but is easier to *generate*, and much more composable.  
* [`InitStates`]({{mkdocs.site_relative_url}}/isplref/#initstates) in particular are often tedious to setup, and it's nice to leverage pythonic iteration or use `math.comb` for generating combinations, etc.

In addition to stuff like this, pythonic-ISPL has additional capabilities, such as support for fragments, so that specifications can be built up gradually.  Specifications can also be sliced apart, and there are other ways they have a [built in algebra](#).  

Let's start with a basic example.

### Minimal Example
<hr style="width:100%;border-bottom:2px solid black;">

The example below is porting the [minimal ISPL example]({{mkdocs.site_relative_url}}/ispl#minimal) to pure python.  

This tries to avoid "magic strings" of raw ISPL and raw logic expressions, and instead makes use of symbols and sympy expressions.

You can run it with the [`ispl` command line tool]({{mkdocs.site_relative_url}}/cli).  

{{site.embed_demo(
    'tests/data/minimal.py', github=github, type='python')}}


{#
Pythonic specifications can also use [AI-powered completion](#)

# An absolutely minimal ISPL program in pure python.
# See the docs https://mattvonrocketstein.github.io/py-mcmas/demos/pythonic-ispl
still requires a do-nothing agent.
# This example is just enough boilerplate to use the logic-engine for simple stuff.
# For the pure-ISPL equivalent, see the file at `tests/data/minimal.ispl`.


# Imports below are optional if file runs with `ispl --python <fname>`:
#
#   from mcmas.ispl import ISPL, Environment, Agent
#   from mcmas.logic import symbols


(including the fact that ISPL fragments allow for 
#}

### Flexibile Parsing
<hr style="width:100%;border-bottom:2px solid black;">

**At a high level,** you'll want to use the pydantic models for [`mcmas.ispl.Environment`]({{mkdocs.site_relative_url}}api/mcmas/ispl.html#Environment) and [`mcmas.ispl.Agent`]({{mkdocs.site_relative_url}}api/mcmas/ispl.html#Agent), which mirror their [counterpart primitives in pure-ISPL]({{mkdocs.site_relative_url}}/isplref#Environment).

**For individual blocks though,** although the JSON schema for *output* is strict, input-parsing is flexible, and *most things that seem to make sense* will just work.  For example,  an `Actions` block in pure ISPL might look like this:

```ispl {.language-lua}
Agent foo 
...
Actions = {keep,swap,none};
...
end Agent
```

<br/>
Translated to python, this could look like 

```python {.language-python .small}
from mcmas.ispl import Agent, symbols 
Agent(...,actions='{keep, swap, none};',...)
Agent(...,actions=['keep', 'swap', 'none'],...)
Agent(...,actions=[symbols.keep, symbols.swap,symbols.none],...)
```

<br/>
The situation is similar with sections like `Vars`, `ObsVars`, and `Lobsvars`, except that dictionaries are used.Most sections accept strings, sympy symbols or expressions that can be converted to strings, lists of strings and symbols, or dictionaries.

### Logical Expressions
<hr style="width:100%;border-bottom:2px solid black;">

We've already met `symbols` in the last section, which is basically just a factory for sympy symbols[^1].  Get a symbol by simply mentioning it, and combine them in natural ways like this:

```python {.small .language-python}
from mcmas.logic import symbols 
assert symbols.x + symbols.y == "x+y"
```

<br/>
Symbols are actually callable, so that e.g. `symbol.K(symbol.predicate)` is one way to use the [knowledge operator]({{mkdocs.site_relative_url}}/opref).  For other kinds of expressions, you can use things from `mcmas.logic` like this:

```python {.small .language-python}
from mcmas import logic
logic.If(symbols.p, symbols.q) # -> p if q
logic.Eq(symbols.x, symbols.y) # -> x=y
logic.And(symbols.x, symbols.y) # -> x and y
```

<br/>
Compared to a simple string, this is frequently *harder* to read!  But it's more composable, and now we're closer to a real abstract syntax tree.

### Original ISPL
<hr style="width:100%;border-bottom:2px solid black;">

The pure ISPL for the python example above:

{{site.embed_demo(
    'tests/data/minimal.ispl', github=github, type='ispl')}}
